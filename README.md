# for_nerdctl

Example Dockerfiles for running containers using nerdctl

These containers are intended for development environments, so the images are kept as small as possible by using Alpine Linux. However, this is not a strict requirement, as developer productivity remains the higher priority
